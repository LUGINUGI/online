<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <title>Skapa Prototyp</title>
    <style>
        :root {
            --primary: #2563eb;
            --primary-hover: #1d4ed8;
            --background: #fafafa;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: system-ui, -apple-system, sans-serif;
        }

        body {
            background: var(--background);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            max-width: 480px;
            width: 90%;
            padding: 2rem;
        }

        h1 {
            color: #1a1a1a;
            font-size: 2.25rem;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .description {
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            color: #374151;
            font-weight: 500;
        }

        input {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #e5e7eb;
            border-radius: 0.5rem;
            background: white;
            transition: all 0.2s;
        }

        input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
        }

        button {
            width: 100%;
            padding: 0.75rem 1.5rem;
            background: linear-gradient(to right, #000000, #1a1a1a);
            color: white;
            border: none;
            border-radius: 0.5rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            position: relative;
            overflow: hidden;
        }

        button:hover {
            transform: scale(1.03);
            background: linear-gradient(to right, #1a1a1a, #2d2d2d);
        }

        button::before {
            content: '';
            position: absolute;
            inset: -2px;
            background: linear-gradient(90deg,
                transparent,
                rgba(255, 255, 255, 0.08),
                transparent
            );
            z-index: 1;
            animation: shimmer 2.5s cubic-bezier(0.4, 0, 0.2, 1) infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>SKAPA PROTOTYP</h1>
        <p class="description">Ange webbadressen till sidan du vill skapa en prototyp av och ett unikt ID för att identifiera den.</p>
        
        <form action="/generate-prototype" method="POST">
            <div class="form-group">
                <label for="url">Webbadress</label>
                <input type="url" id="url" name="url" required 
                       placeholder="https://exempel.com">
            </div>
            
            <div class="form-group">
                <label for="identifier">Undersida</label>
                <input type="text" id="identifier" name="identifier" required 
                       placeholder="namn">
            </div>
            
            <button type="submit">Skapa Prototyp</button>
        </form>

        <div id="thankYouMessage" style="display: none; text-align: center; margin-top: 1rem; color: #666;">
            Tack! Du kommer att omdirigeras om 5 sekunder.
        </div>

        <script>
        document.querySelector('form').addEventListener('submit', function(e) {
            document.getElementById('thankYouMessage').style.display = 'block';
        });
        </script>
    </div>
</body>
</html>
